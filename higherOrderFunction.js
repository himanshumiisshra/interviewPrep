//higher order function ---- the functions that can either take a function as an argument or return a function as a result


const number = [1,2,3,4,5]
const doubles = number.map(function (num)  {
    return num * 2;
})